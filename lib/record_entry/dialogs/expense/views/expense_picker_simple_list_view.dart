import 'package:flutter/material.dart';
import '../expense_sheet_config.dart';

class ExpensePickerSimpleListView extends StatelessWidget {
  final ExpenseDialogConfig config;

  const ExpensePickerSimpleListView({super.key, required this.config});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TODO: 单列表选择模式'));
  }
}
